#include<stdio.h>
void main()
{
    int i,first,second;
    int a[10] = {1,2,3,4,10,6,7,8};

    for (i = 0; i <8; i++)
    {
        printf("%d",a[i]);
        if (a[0] < a[i])
        {
            /* code */a[0] = a[i];
           
        }
        
    }
    printf("\n large :%d",a[0]);
    first = a[0];

    for ( i = 0; i < 8 i++)
    {
        if (first<a)
        {
            /* code */
        }
        
    }
    
}